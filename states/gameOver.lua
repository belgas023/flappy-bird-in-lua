local gameOver = {
    name = 'Game Over'
}

local bird = require('bird')

function gameOver.load()

end

function gameOver.update(dt)
    bird.update(dt)
end

function gameOver.draw()

end

return gameOver
